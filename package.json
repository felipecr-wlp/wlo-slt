{
  "name": "wlo-slt",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "supabase db push",
    "db:reset": "supabase db reset",
    "db:seed": "supabase db reset --linked",
    "migration:new": "supabase migration new",
    "tracker:build": "pnpm --filter=tracker build"
  },
  "devDependencies": {
    "supabase": "^2.11.0",
    "turbo": "^2.10.10"
  },
  "engines": {
    "packageManager": "pnpm@9.x"
  }
}
